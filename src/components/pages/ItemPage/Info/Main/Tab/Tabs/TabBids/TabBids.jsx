import React from "react";
import styled from "styled-components";

const TabBids = ({ homePath }) => {
  return <Wrapper>xx</Wrapper>;
};

const Wrapper = styled.div`
  /* background-color: red; */
  height: 100%;

  flex: 1;
`;

export default TabBids;
