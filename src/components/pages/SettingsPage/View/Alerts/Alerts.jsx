import React from "react";
import styled from "styled-components";

const Alerts = (props) => {
  return <Wrapper>Alerts</Wrapper>;
};

const Wrapper = styled.div`
  background-color: green;
`;

export default Alerts;
