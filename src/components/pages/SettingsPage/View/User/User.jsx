import React from "react";
import styled from "styled-components";

const User = (props) => {
  return <Wrapper>userrrr</Wrapper>;
};

const Wrapper = styled.div`
  background-color: green;
`;

export default User;
